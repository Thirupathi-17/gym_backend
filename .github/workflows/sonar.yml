name: Debug Backend Structure

on:
  push:
    branches:
      - main

jobs:
  debug:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Show current directory
        run: pwd

      - name: Show full folder tree
        run: ls -R



